<UserControl x:Class="SchoolSchedule.App.Views.PlaceholderPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <materialDesign:PackIcon Kind="{Binding Icon, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  Width="64" Height="64" HorizontalAlignment="Center"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}" />
        <TextBlock Text="{Binding SectionTitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="22" FontWeight="Medium" Margin="0,16,0,4" HorizontalAlignment="Center" />
        <TextBlock Text="Раздел появится на следующем этапе разработки"
                   FontSize="14" Opacity="0.6" HorizontalAlignment="Center" />
    </StackPanel>
</UserControl>
