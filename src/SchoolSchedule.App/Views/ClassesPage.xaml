<UserControl x:Class="SchoolSchedule.App.Views.ClassesPage"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:helpers="clr-namespace:SchoolSchedule.App.Helpers"
             mc:Ignorable="d"
             Loaded="ClassesPage_OnLoaded"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Классы" Style="{StaticResource PageTitleStyle}" />
            <Button DockPanel.Dock="Right" HorizontalAlignment="Right"
                    Style="{StaticResource AppRaisedButton}"
                    Command="{Binding AddCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" Margin="0,0,8,0" />
                    <TextBlock Text="Добавить класс" />
                </StackPanel>
            </Button>
        </DockPanel>

        <materialDesign:Card Grid.Row="1" Padding="0">
            <DataGrid ItemsSource="{Binding Classes}"
                      SelectedItem="{Binding SelectedClass}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      RowEditEnding="DataGrid_OnRowEditEnding">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Класс" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="1.5*" />
                    <DataGridComboBoxColumn Header="Смена" ItemsSource="{x:Static helpers:EnumSource.Shifts}"
                                             SelectedItemBinding="{Binding Shift}" Width="*" />
                    <DataGridTextColumn Header="Учеников" Binding="{Binding StudentCount, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                    <DataGridTemplateColumn Header="Классный кабинет" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding HomeRoom.Name, TargetNullValue='— не назначен —'}"
                                           VerticalAlignment="Center" Margin="8,0" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.AllRooms, ElementName=Root}"
                                          SelectedValuePath="Id" DisplayMemberPath="Name"
                                          SelectedValue="{Binding HomeRoomId, UpdateSourceTrigger=PropertyChanged}"
                                          IsEditable="False" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                        Command="{Binding DataContext.DeleteCommand, ElementName=Root}"
                                        CommandParameter="{Binding}"
                                        ToolTip="Удалить класс">
                                    <materialDesign:PackIcon Kind="Delete" />
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
