<Application x:Class="SchoolSchedule.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:SchoolSchedule.App.Converters"
             xmlns:helpers="clr-namespace:SchoolSchedule.App.Helpers"
             xmlns:local="clr-namespace:SchoolSchedule.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="Brown" SecondaryColor="Amber" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Тёплый нейтральный фон вместо стандартного холодно-серого — под бежевую гамму -->
            <SolidColorBrush x:Key="MaterialDesignBackground" Color="#F6F1E7" />
            <SolidColorBrush x:Key="MaterialDesignPaper" Color="#FFFDFA" />

            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
            <converters:NullToInverseVisibilityConverter x:Key="NullToInverseVisibilityConverter" />
            <helpers:NullToRoomTypeSentinelConverter x:Key="NullToRoomTypeSentinelConverter" />

            <!-- Единый вид заголовка раздела на каждой странице -->
            <Style x:Key="PageTitleStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="26" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>

            <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Opacity" Value="0.87" />
                <Setter Property="Margin" Value="0,0,0,10" />
            </Style>

            <!-- Чуть просторнее и крупнее шрифт в таблицах, чем в стандартном Material-стиле -->
            <Style TargetType="DataGrid" BasedOn="{StaticResource MaterialDesignDataGrid}">
                <Setter Property="RowHeight" Value="46" />
                <Setter Property="ColumnHeaderHeight" Value="42" />
                <Setter Property="FontSize" Value="15" />
                <Setter Property="GridLinesVisibility" Value="Horizontal" />
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource MaterialDesignDivider}" />
            </Style>

            <!-- Более крупные скругления по всему приложению — карточки, кнопки, поля ввода -->
            <Style TargetType="materialDesign:Card">
                <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp3" />
                <Setter Property="UniformCornerRadius" Value="16" />
                <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}" />
            </Style>

            <!-- Свои ключи (не переопределяем ключи темы напрямую — с BasedOn на самого себя
                 Material-стиль ломается и кнопка остаётся вообще без стиля). -->
            <Style x:Key="AppRaisedButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="12" />
            </Style>

            <Style x:Key="AppOutlinedButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
                <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="12" />
            </Style>

            <Style x:Key="AppFlatButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="10" />
            </Style>

            <!-- Поля ввода — контурные, со скруглёнными углами вместо стандартной линии снизу -->
            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
                <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="10" />
            </Style>

            <Style TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
                <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="10" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
